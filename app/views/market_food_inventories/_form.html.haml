= simple_form_for @market_food_inventory, html:{class: 'form-horizontal', role: 'form'} do |f|
  = form_error_notification(f)
  = f.input :item_name
  = f.input :quantity
  .form-group
    .col-sm-offset-2.col-sm-10
      = f.button :submit
